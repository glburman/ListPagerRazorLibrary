@model ListPagerModel
<ul id="ul-@Model.PageSizeInputId" class="pagination">
    <li class="page-item pager-page-size">
        <span class="d-inline">
            Page Size:
        </span>
        <span class="d-inline">
            <input id="@Model.PageSizeInputId"
                   name="@Model.PageSizeInputId"
                   type="number"
                   min="@Model.MinPageSize"
                   max="@Model.MaxPageSize"
                   onChange="pager.setPageSize(this.value)"
                   class="d-inline form-control-sm text-center pager-page-size"
                   value="@Model.PageSize">
        </span>
    </li>
</ul>
